#!/usr/bin/python3

def number_keys(a_dictionary):
    """ The number of keys in dictionaries
        Args:
            a_dictionary: a dictionary
        Returns:
            the number of keys
    """

    return (len(a_dictionary))
